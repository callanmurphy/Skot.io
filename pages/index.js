import { useRouter } from 'next/router'

export default function Home() {

    const router = useRouter()

    return (
        <div>
          <title>Skot</title>
          <h1>Skot</h1>
        </div>
      )
}